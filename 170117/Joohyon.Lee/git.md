#버전 관리란
```거의 대부분의 주요 소프트웨어 개발 프로젝트는 아직도 소프트웨어의 설계도라 할 수 있는 소스 코드 작성이 
주요한 부분이 되며 이러한 소스 코드는 기업체 또는 연구소의 핵심 역량이 응축된 핵심 자산이다. 

따라서 어떠 형태로든 소스 코드를 백업하여 분실의 위험에서 보호하고 개정 전후 내용을 파악하여 추후 발생할지도 모를 오류 수정에 대비하는 절차가 필요하다. 

버전 관리 소프트웨어는 조직의 핵심 자산인 소스 코드의 개정과 백업 절차를 자동화하여 오류 수정 과정을 도와줄 수 있는 시스템으로 이미 다수의 국제 협력 개방 소프트웨어 개발 실무에서도 널리 사용되고 있다. 
다음은 버전 관리 시스템을 사용하는 원인을 정리한 것이다.

*무언가 잘못되었을 때 복구를 돕기 위하여
*프로젝트 진행 중 과거의 어떤 시점으로 돌아갈 수 있게 하기 위하여
*여러사람이 같은 프로젝트에 참여할 경우, 각자가 수정한 부분을 팀원 전체가 동기화하는 과정을 자동화하기 위하여
*소스 코드의 변경 사항을 추적하기 위하여
*소스 코드에서 누가 수정했는지 추적하기 위하여
*대규모 수정 작업을 더욱 안전하게 진행하기 위하여
*가지내기(Branch)로 프로젝트에 영향을 최소화 하면서 새로운 부분을 개발하기 위하여
*접붙이기(Merge)로 검증이 끝난 후 새로이 개발된 부분을 본류(trunk)에 합치기 위하여
*많은 오픈 소스 프로젝트에서 어떠한 형태로든 버전 관리를 사용하고 있으므로
*코드의 특정 부분이 왜 그렇게 쓰여 졌는지 의미를 추적하기 위하여```

#Git 실행 
로컬에 저장소 -> github 연결 -> init -> .gitignore -> 첫번쨰 커밋 

 `<git clone url: address  : github에 올라가잇는 파일정보를 내려받아올때>`

클론 복제

`git diff : 수정 사항을 볼 수 있다. `

#Git 명령어


1. **스테이지에 추가 add**

`git add : 워킹 디렉토리에서 수정된 사항을 stage에 올린다.`

 `git add -A : 수정된 모든 사항을 모두 stage에 올린다.`


2. **사용자 설정 config**  
 
`<git config --global user.email "jhe0956@gmail.com"> : 사용자 이메일 설정`

`<git config --global user.name "jhe0956@gmail.com"> : 사용자 이름 설정` 


3.  
 `git commit 파일명 : 수정했던 사항을 커밋한다.`

`git commit -a(전체 파일) -m "" : git add 하지 않고 한번에 commit 할 수 있다.`


4. reset : 이루어 졌던 커밋을 삭제하면  지정했던 커밋 부분으로 리셋한다. 

`git reset --hard (리셋의 방법 3가지) 커밋번호 : 커밋번호의 상태로 리셋 한다..`

.`git reset --hard (리셋의 방법 3가지) 커밋번호 : 커밋번호의 상태로 리셋한다.`

`git reset --hard ORIG_HEAD : 리셋한 상태에서 다시 최근  커밋으로  리셋한다. `

`< git reset HEAD 파일명 : 복원 시킨다. (HEAD는 최근 상태 ) >`

5. revert : 선택한  커밋을 취소 한다.  ( 한개이상 취소하면 높은 확률로 conflict)

`git revert  [커밋명 ]` 


**.gitignore** 시작하자마자 해야한다.그래야  나중에 다른 파일들과 conflict가 안나온다.







##branch
`git branch [새로만들 브랜치명] 부모가될 주소 `

`git checkout [옮겨갈 브랜치명] 다른 브런치로 넘어가는것 : commit을 하지 않고 다른 branch로 넘어간다면 commit사항까지 다른 branch로 넘어간다.`  
`git checkout (다른브랜치명)`

##Merge
`git merge : 원하는 branch 병합 `

합칠려는 자식 branch에서 commit 번호를 복사후 합칠려는 branch 로와서 merge 

merge 후 conflict 수정 후 완료 

confilct 가 안난다면 commit을 안해도 된다.

git show-branch : branch의 관계를 확인하는 

##graph(ubuntu는 gitkraken)
git log --graph --all 트리구조를  한번에 확인 할 수 있는 명렁
